/** Assessment panels fill the viewport column below the page header (desktop). */
export const assessmentPanelHeightClass = "h-full min-h-0 max-h-full";

/** Section wrapper keeps the assessment wizard within the viewport. */
export const assessmentViewportSectionClass =
  "flex flex-col overflow-hidden !pb-4 lg:h-[calc(100vh-5rem)] lg:max-h-[calc(100vh-5rem)]";

export const assessmentViewportContentClass = "flex min-h-0 flex-1 flex-col";
